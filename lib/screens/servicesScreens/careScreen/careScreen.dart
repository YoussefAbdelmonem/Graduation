import 'package:flutter/cupertino.dart';

class CareScreen extends StatelessWidget {
  const CareScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Care Screen'),);
  }
}
